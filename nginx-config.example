# Configuração do Nginx para alya.sistemas.viverdepj.com.br
# Salvar em: /etc/nginx/sites-available/alya.sistemas.viverdepj.com.br

server {
    listen 80;
    server_name alya.sistemas.viverdepj.com.br;

    # Logs
    access_log /var/log/nginx/alya-access.log;
    error_log /var/log/nginx/alya-error.log;

    # Frontend - arquivos estáticos
    root /www/alya/dist;
    index index.html;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/json;

    # Frontend - React Router
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API Backend
    location /api {
        proxy_pass http://localhost:8001/api;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Upload de arquivos
    client_max_body_size 10M;
}

# Após configurar SSL com Certbot, esta seção será adicionada automaticamente:
# server {
#     listen 443 ssl http2;
#     server_name alya.sistemas.viverdepj.com.br;
#     
#     ssl_certificate /etc/letsencrypt/live/alya.sistemas.viverdepj.com.br/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/alya.sistemas.viverdepj.com.br/privkey.pem;
#     
#     # ... resto da configuração igual acima ...
# }
# 
# # Redirecionar HTTP para HTTPS
# server {
#     listen 80;
#     server_name alya.sistemas.viverdepj.com.br;
#     return 301 https://$server_name$request_uri;
# }

